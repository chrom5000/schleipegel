# 3D-Hero Phase 1 (Grundszene) — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** MapLibre-3D-Grundszene der Schlei mit echtem Unterwasser-Relief (gebackene Kombi-Geländekacheln), Kamera-Presets, Seekarten-Umschalter (FNC) und Silhouetten-Fallback.

**Architecture:** Selbst gehostetes MapLibre GL JS rendert in `#hero3d` über der bestehenden SVG-Silhouette; `hero3d.js` (eigene Datei, klassisches Script) kapselt Styles, Kamera und Umschalter. Gelände kommt aus einmalig gebackenen, committeten Terrarium-Kacheln (Land: AWS-DEM, Seegrund: BSH/EMODnet-Bathymetrie) — zur Laufzeit keine externe Relief-Abhängigkeit.

**Tech Stack:** MapLibre GL JS (gepinnt, vendored), Python 3 mit `requests`/`numpy`/`pillow`/`rasterio` (nur Bake-Zeit), puppeteer-core für Verifikation.

**Spec:** `.claude/specs/2026-07-12-3d-hero-design.md`

## Global Constraints

- Kein Build-Schritt, kein CDN zur Laufzeit; neue Assets mit `?v=__BUILD__`.
- Jeder Push auf `main` geht live (dieschlei.de) — committen ja, **pushen nur auf Nutzeranweisung**; vorher `git pull --rebase` (Badewasser-Cron).
- **Phase-1-Rollout:** Standardansicht bleibt vorerst die Silhouette; die 3D-Szene ist über den Umschalter erreichbar (Zyklus Silhouette → Revier 3D → Seekarte). Erst Phase 2 (Datenlayer vorhanden) macht 3D zum Standard. Grund: Phase 1 hätte sonst eine Standardansicht ohne Wind-/Pegeldaten.
- UI-Texte, Kommentare, Commits: Deutsch. Bestehende Idiome: Loader-schreiben-`state`/Renderer-idempotent; Abschnitts-Banner `/* ── … ── */`.
- Kein WebGL / `prefers-reduced-motion` / Ladefehler → Silhouette bleibt, 3D-Bedienelemente unsichtbar.
- `GEO_BBOX` (app.js:845): lon 9.5439575…10.0361676, lat 54.4900432…54.6941334.

---

### Task 1: Spike — Datenlage Bathymetrie & FNC-Zoomstufen

**Files:**
- Create: `.claude/specs/2026-07-12-terrain-spike.md` (Ergebnisnotiz)

**Interfaces:**
- Produces: Entscheidung `BATHY_MODE` (`"bsh-wcs"` oder `"emodnet"`) und nutzbare FNC-Zoomspanne — beides wird in Task 3/5 als Konstante eingetragen.

- [ ] **Step 1: BSH-WCS prüfen (Rohdaten statt Kartenbild)**

```bash
curl -s "https://gdi.bsh.de/mapservice_gs/ELC_INSPIRE/ows?service=WCS&version=2.0.1&request=GetCapabilities" | head -80
```

Erwartet: XML mit `wcs:Capabilities` und Coverage-Liste. Dann pro gefundener Coverage-ID:

```bash
curl -s "https://gdi.bsh.de/mapservice_gs/ELC_INSPIRE/ows?service=WCS&version=2.0.1&request=DescribeCoverage&coverageId=<ID>" | head -60
```

GetCoverage-Test für die innere Schlei (Kleine Breite), GeoTIFF ins Scratchpad:

```bash
curl -s "https://gdi.bsh.de/mapservice_gs/ELC_INSPIRE/ows?service=WCS&version=2.0.1&request=GetCoverage&coverageId=<ID>&subset=Long(9.54,9.66)&subset=Lat(54.49,54.55)&format=image/tiff" -o "$SCRATCH/bsh_test.tif"
python3 -c "import rasterio; d=rasterio.open('$SCRATCH/bsh_test.tif').read(1); import numpy as np; print(d.shape, np.nanmin(d), np.nanmax(d), (~np.isnan(d)).mean())"
```

Erwartet bei Erfolg: negative Tiefenwerte, Abdeckungsquote > 0,5 in der inneren Schlei.

- [ ] **Step 2: EMODnet-WCS als Fallback prüfen**

```bash
curl -s "https://ows.emodnet-bathymetry.eu/wcs?service=WCS&version=2.0.1&request=GetCoverage&coverageId=emodnet__mean&subset=Long(9.54,9.66)&subset=Lat(54.49,54.55)&format=image/tiff" -o "$SCRATCH/emod_test.tif"
```

Gleiche rasterio-Prüfung. EMODnet (~115-m-Gitter) gilt als ausreichend, wenn BSH die innere Schlei nicht flächig abdeckt.

- [ ] **Step 3: FNC-Zoomstufen über der Schlei prüfen**

```bash
node -e 'const lat=54.58,lon=9.8;for(const z of[8,10,12,14,15,16,17]){const x=Math.floor((lon+180)/360*2**z),r=lat*Math.PI/180,y=Math.floor((1-Math.log(Math.tan(r)+1/Math.cos(r))/Math.PI)/2*2**z);console.log(z,x,y)}' \
| while read z x y; do printf "z%s: %s bytes\n" "$z" "$(curl -s "https://freenauticalchart.net/fnc-de/$z/$x/$y.png" | wc -c)"; done
```

Erwartet: >1000 Bytes in den nutzbaren Stufen; leere/404-Stufen notieren → ergibt `minzoom`/`maxzoom` der FNC-Quelle.

- [ ] **Step 4: Ergebnisnotiz schreiben und committen**

`.claude/specs/2026-07-12-terrain-spike.md` mit: gewähltem `BATHY_MODE`, Coverage-ID bzw. EMODnet-Begründung, Abdeckungsquote innere Schlei, FNC-Zoomspanne, Auffälligkeiten.

```bash
git add .claude/specs/2026-07-12-terrain-spike.md && git commit -m "Spike: Bathymetrie-Datenlage und FNC-Zoomstufen für den 3D-Hero"
```

---

### Task 2: Wasser-GeoJSON aus schlei-geo.js backen

**Files:**
- Create: `scripts/bake_water_geojson.py`
- Create: `water.geojson`

**Interfaces:**
- Consumes: `schlei-geo.js` (SVG-Pfad, nur `M`/implizite `L`/`Z`, equirektangular; viewBox 1000×715,6), `GEO_BBOX`-Werte aus den Global Constraints.
- Produces: `water.geojson` — FeatureCollection mit einem MultiPolygon `{"name":"schlei-wasser"}` in Lon/Lat (WGS84). Wird in Task 5 als GeoJSON-Quelle `water` geladen.

- [ ] **Step 1: Skript schreiben**

```python
#!/usr/bin/env python3
"""Rueckprojektion der gebackenen Schlei-Wasserflaeche nach Lon/Lat.

Der SVG-Pfad in schlei-geo.js ist linear (equirektangular) auf die
viewBox 1000 x 715.6 projiziert — die Umkehrung ist exakt.
Nur Bake-Zeit; Ausgabe water.geojson wird committet.
"""
import json, re, sys

BBOX = dict(lon0=9.5439575, lat0=54.4900432, lon1=10.0361676, lat1=54.6941334, w=1000.0, h=715.6)

src = open('schlei-geo.js', encoding='utf-8').read()
path = json.loads(re.search(r'const SCHLEI_GEO = (\{.*\})', src, re.S).group(1).rsplit(';', 1)[0])['path']
assert not re.search(r'[A-LN-YA-Za-ln-y]', path.replace('M', '').replace('Z', '').replace('z', '')), \
    'Pfad enthaelt unerwartete SVG-Kommandos'

def to_lonlat(x, y):
    lon = BBOX['lon0'] + x / BBOX['w'] * (BBOX['lon1'] - BBOX['lon0'])
    lat = BBOX['lat1'] - y / BBOX['h'] * (BBOX['lat1'] - BBOX['lat0'])
    return [round(lon, 6), round(lat, 6)]

polys = []
for sub in filter(None, (s.strip() for s in path.replace('Z', 'z').split('z'))):
    assert sub.startswith('M'), sub[:20]
    pts = [to_lonlat(*map(float, p.split(','))) for p in sub[1:].split()]
    if pts[0] != pts[-1]:
        pts.append(pts[0])
    polys.append([pts])

fc = {"type": "FeatureCollection", "features": [{
    "type": "Feature", "properties": {"name": "schlei-wasser"},
    "geometry": {"type": "MultiPolygon", "coordinates": polys}}]}
json.dump(fc, open('water.geojson', 'w'), separators=(',', ':'))
print(f'{len(polys)} Polygone, {sum(len(p[0]) for p in polys)} Punkte')
```

- [ ] **Step 2: Laufen lassen und Plausibilität prüfen**

```bash
python3 scripts/bake_water_geojson.py
python3 -c "import json; g=json.load(open('water.geojson')); c=g['features'][0]['geometry']['coordinates']; xs=[p[0] for poly in c for p in poly[0]]; ys=[p[1] for poly in c for p in poly[0]]; print(min(xs),max(xs),min(ys),max(ys))"
```

Erwartet: Lon zwischen 9.54 und 10.04, Lat zwischen 54.49 und 54.70 (≈ GEO_BBOX).

- [ ] **Step 3: Commit**

```bash
git add scripts/bake_water_geojson.py water.geojson && git commit -m "Wasserfläche als GeoJSON gebacken (Rückprojektion aus schlei-geo.js)"
```

---

### Task 3: Kombi-Geländekacheln backen (Land + Seegrund)

**Files:**
- Create: `scripts/bake_terrain.py`
- Create: `terrain/{z}/{x}/{y}.png` (z 9–13, Bake-Bbox lon 9.40–10.20, lat 54.40–54.78)

**Interfaces:**
- Consumes: `BATHY_MODE` + Coverage-ID/URL aus Task 1 (im Skript als Konstante eintragen).
- Produces: Terrarium-kodierte Kacheln unter `terrain/…` — Task 5 referenziert sie als raster-dem-Quelle `{tiles:["terrain/{z}/{x}/{y}.png"], encoding:"terrarium", minzoom:9, maxzoom:13}`.

- [ ] **Step 1: Skript schreiben**

```python
#!/usr/bin/env python3
"""Kombi-Gelaendekacheln fuer den 3D-Hero: Land (AWS-Terrarium-DEM)
plus Seegrund (Bathymetrie) in eigene Terrarium-PNGs backen.

Nur Bake-Zeit. Abhaengigkeiten: pip3 install requests numpy pillow rasterio
Quellen: AWS Terrain Tiles (Namensnennung), BSH (DL-DE-BY-2.0) bzw.
EMODnet Bathymetry. Ausgabe terrain/{z}/{x}/{y}.png wird committet.
"""
import io, math, os, sys
import numpy as np
import requests
from PIL import Image
import rasterio

BBOX = (9.40, 54.40, 10.20, 54.78)          # lon0, lat0, lon1, lat1
ZOOMS = range(9, 14)
BATHY_MODE = 'TODO-aus-Spike'               # 'bsh-wcs' | 'emodnet'  ← Task-1-Ergebnis eintragen
BSH_COVERAGE_ID = 'TODO-aus-Spike'
DEM_URL = 'https://s3.amazonaws.com/elevation-tiles-prod/terrarium/{z}/{x}/{y}.png'

WCS = {
    'bsh-wcs': ('https://gdi.bsh.de/mapservice_gs/ELC_INSPIRE/ows?service=WCS&version=2.0.1'
                f'&request=GetCoverage&coverageId={BSH_COVERAGE_ID}'
                '&subset=Long({x0},{x1})&subset=Lat({y0},{y1})&format=image/tiff'),
    'emodnet': ('https://ows.emodnet-bathymetry.eu/wcs?service=WCS&version=2.0.1'
                '&request=GetCoverage&coverageId=emodnet__mean'
                '&subset=Long({x0},{x1})&subset=Lat({y0},{y1})&format=image/tiff'),
}

def tile_range(z):
    def xy(lon, lat):
        r = math.radians(lat)
        n = 2 ** z
        return (int((lon + 180) / 360 * n),
                int((1 - math.log(math.tan(r) + 1 / math.cos(r)) / math.pi) / 2 * n))
    x0, y1 = xy(BBOX[0], BBOX[1]); x1, y0 = xy(BBOX[2], BBOX[3])
    return range(x0, x1 + 1), range(y0, y1 + 1)

def tile_lonlat_grid(z, x, y, size=256):
    n = 2 ** z
    xs = (x + (np.arange(size) + 0.5) / size) / n * 360 - 180
    ymerc = (y + (np.arange(size) + 0.5) / size) / n
    lats = np.degrees(np.arctan(np.sinh(math.pi * (1 - 2 * ymerc))))
    return np.meshgrid(xs, lats)

def decode_terrarium(img):
    a = np.asarray(img.convert('RGB'), dtype=np.float64)
    return a[..., 0] * 256 + a[..., 1] + a[..., 2] / 256 - 32768

def encode_terrarium(elev):
    v = np.clip(elev, -11000, 8900) + 32768
    r = np.floor(v / 256); g = np.floor(v % 256); b = np.floor((v - np.floor(v)) * 256)
    return Image.fromarray(np.stack([r, g, b], axis=-1).astype(np.uint8))

print('Bathymetrie laden …')
url = WCS[BATHY_MODE].format(x0=BBOX[0], y0=BBOX[1], x1=BBOX[2], y1=BBOX[3])
tif = requests.get(url, timeout=120); tif.raise_for_status()
ds = rasterio.open(io.BytesIO(tif.content))
bathy = ds.read(1).astype(np.float64)
nodata = ds.nodata
if nodata is not None:
    bathy[bathy == nodata] = np.nan
# EMODnet/BSH liefern Tiefe als negative Hoehe; positive Werte sind Artefakte → verwerfen
bathy[bathy >= 0] = np.nan
inv = ~ds.transform  # Lon/Lat → Pixel

def bathy_at(lons, lats):
    cols, rows = inv * (lons, lats)
    cols = np.round(cols).astype(int); rows = np.round(rows).astype(int)
    ok = (rows >= 0) & (rows < bathy.shape[0]) & (cols >= 0) & (cols < bathy.shape[1])
    out = np.full(lons.shape, np.nan)
    out[ok] = bathy[rows[ok], cols[ok]]
    return out

total = 0
for z in ZOOMS:
    xr, yr = tile_range(z)
    for x in xr:
        for y in yr:
            r = requests.get(DEM_URL.format(z=z, x=x, y=y), timeout=60)
            r.raise_for_status()
            elev = decode_terrarium(Image.open(io.BytesIO(r.content)))
            lons, lats = tile_lonlat_grid(z, x, y)
            depth = bathy_at(lons, lats)
            use = ~np.isnan(depth) & (elev <= 1.0)   # nur Wasserflaeche ersetzen, Land unangetastet
            elev[use] = depth[use]
            os.makedirs(f'terrain/{z}/{x}', exist_ok=True)
            encode_terrarium(elev).save(f'terrain/{z}/{x}/{y}.png', optimize=True)
            total += 1
    print(f'z{z}: fertig')
print(f'{total} Kacheln geschrieben')
```

- [ ] **Step 2: `BATHY_MODE`/Coverage-ID aus dem Spike eintragen, laufen lassen**

```bash
pip3 install --quiet requests numpy pillow rasterio && python3 scripts/bake_terrain.py
```

Erwartet: `… Kacheln geschrieben` (Größenordnung 150–200), Gesamtgröße prüfen:

```bash
du -sh terrain/ && find terrain -name '*.png' | wc -l
```

Erwartet: < 15 MB (sonst ZOOMS auf 9–12 reduzieren und neu backen).

- [ ] **Step 3: Stichprobe — hat die Fahrrinne echte Tiefe?**

```bash
python3 - <<'EOF'
import numpy as np
from PIL import Image
a = np.asarray(Image.open('terrain/13/4318/2607.png').convert('RGB'), dtype=float)  # Missunder Enge
elev = a[...,0]*256 + a[...,1] + a[...,2]/256 - 32768
print('min', elev.min(), 'max', elev.max())
EOF
```

Erwartet: `min` deutlich negativ (Rinne bei Missunde ~ −10 m und tiefer), `max` positiv (Uferland).

- [ ] **Step 4: Commit**

```bash
git add scripts/bake_terrain.py terrain/ && git commit -m "Kombi-Geländekacheln gebacken: Land (AWS-DEM) + Seegrund (Bathymetrie)"
```

---

### Task 4: MapLibre GL JS vendoren

**Files:**
- Create: `vendor/maplibre-gl.js`, `vendor/maplibre-gl.css`, `vendor/VERSION`

**Interfaces:**
- Produces: global `maplibregl` (klassisches Script). Task 5 lädt `vendor/maplibre-gl.js?v=__BUILD__` dynamisch.

- [ ] **Step 1: Aktuelle Version ermitteln, Dateien holen, Version festhalten**

```bash
V=$(npm view maplibre-gl version) && echo "$V" \
  && mkdir -p vendor \
  && curl -sfL "https://unpkg.com/maplibre-gl@$V/dist/maplibre-gl.js" -o vendor/maplibre-gl.js \
  && curl -sfL "https://unpkg.com/maplibre-gl@$V/dist/maplibre-gl.css" -o vendor/maplibre-gl.css \
  && echo "maplibre-gl $V (BSD-3-Clause, https://maplibre.org)" > vendor/VERSION
```

- [ ] **Step 2: Plausibilität prüfen**

```bash
node -e "const s=require('fs').readFileSync('vendor/maplibre-gl.js','utf8'); console.log(s.length, /maplibregl/.test(s))"
```

Erwartet: > 500000 Zeichen und `true`.

- [ ] **Step 3: Commit**

```bash
git add vendor/ && git commit -m "MapLibre GL JS vendored (Version siehe vendor/VERSION)"
```

---

### Task 5: hero3d.js — Grundszene, Kamera, Umschalter-Logik

**Files:**
- Create: `hero3d.js`
- Modify: `index.html` (Container + Script-Referenz, siehe Steps)
- Modify: `styles.css` (Container-/Fade-Regeln)

**Interfaces:**
- Consumes: `terrain/{z}/{x}/{y}.png` (Task 3), `water.geojson` (Task 2), `vendor/maplibre-gl.js` (Task 4), FNC-Zoomspanne aus Task 1 (Konstante `FNC`).
- Produces: `window.HERO3D = { init(), setMode(mode), getMode(), setPreset(name), ready }` mit `mode ∈ 'lite'|'3d'|'chart'` und Preset-Namen `'schlei'|'innere'|'enge'|'kappeln'|'muendung'`. Task 6 (Buttons) und Phase 2 (Datenlayer) bauen darauf.

- [ ] **Step 1: hero3d.js schreiben**

```javascript
/* ═══════════════════════════════════════════════════════════════
   HERO3D — MapLibre-Szene „Revier 3D" + Seekarten-Ansicht.
   Läuft neben app.js, liest nichts aus state (Phase 1).
   Modi: 'lite' (SVG-Silhouette, Standard in Phase 1), '3d', 'chart'.
   ═══════════════════════════════════════════════════════════════ */

'use strict';

(() => {
  const BUILD = document.querySelector('script[src^="app.js"]')?.src.split('v=')[1] ?? 'dev';
  const FNC = { min: 7, max: 16 };                    // ← Spike-Ergebnis (Task 1) eintragen
  const MAX_BOUNDS = [[9.35, 54.38], [10.35, 54.80]];

  const PRESETS = {
    schlei:   { center: [9.79, 54.585], zoom: 10.3, pitch: 55, bearing: 20 },
    innere:   { center: [9.60, 54.520], zoom: 12.0, pitch: 60, bearing: 30 },
    enge:     { center: [9.665, 54.532], zoom: 13.0, pitch: 60, bearing: 0 },
    kappeln:  { center: [9.955, 54.650], zoom: 12.5, pitch: 55, bearing: -20 },
    muendung: { center: [10.03, 54.670], zoom: 13.0, pitch: 55, bearing: -30 },
  };

  const STYLE_3D = {
    version: 8,
    sources: {
      terrain: { type: 'raster-dem', tiles: [`terrain/{z}/{x}/{y}.png?v=${BUILD}`],
                 encoding: 'terrarium', tileSize: 256, minzoom: 9, maxzoom: 13,
                 bounds: [9.40, 54.40, 10.20, 54.78],
                 attribution: 'Relief: BSH (DL-DE-BY-2.0) · Terrain Tiles' },
      water: { type: 'geojson', data: `water.geojson?v=${BUILD}` },
    },
    layers: [
      { id: 'bg', type: 'background', paint: { 'background-color': '#0d1b22' } },
      { id: 'relief', type: 'hillshade', source: 'terrain',
        paint: { 'hillshade-shadow-color': '#050d12', 'hillshade-highlight-color': '#3a6a86',
                 'hillshade-accent-color': '#12242e', 'hillshade-exaggeration': 0.7 } },
      { id: 'wasser', type: 'fill', source: 'water',
        paint: { 'fill-color': '#2478ad', 'fill-opacity': 0.35 } },
      { id: 'ufer', type: 'line', source: 'water',
        paint: { 'line-color': '#58b7e8', 'line-width': 1, 'line-opacity': 0.7 } },
    ],
    terrain: { source: 'terrain', exaggeration: 2.5 },
  };

  const STYLE_CHART = {
    version: 8,
    sources: {
      fnc: { type: 'raster', tiles: ['https://freenauticalchart.net/fnc-de/{z}/{x}/{y}.png'],
             tileSize: 256, minzoom: FNC.min, maxzoom: FNC.max,
             attribution: '© FreeNauticalChart (BSH-Daten) — nicht zur Navigation' },
    },
    layers: [
      { id: 'bg', type: 'background', paint: { 'background-color': '#dfe9ee' } },
      { id: 'seekarte', type: 'raster', source: 'fnc' },
    ],
  };

  let map = null;
  let mode = 'lite';

  function supported() {
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return false;
    try {
      const c = document.createElement('canvas');
      return !!(c.getContext('webgl2') || c.getContext('webgl'));
    } catch { return false; }
  }

  function setDom(newMode) {
    mode = newMode;
    document.documentElement.dataset.heroMode = newMode;   // CSS blendet Silhouette/Canvas um
    try { localStorage.setItem('hero-view', newMode); } catch { /* privat-Modus */ }
    const cap = document.querySelector('#hero3d-caption');
    if (cap) cap.hidden = newMode !== 'chart';
  }

  function ensureMap() {
    if (map) return map;
    map = new maplibregl.Map({
      container: 'hero3d',
      style: STYLE_3D,
      ...PRESETS.schlei,
      maxBounds: MAX_BOUNDS, minZoom: 9, maxZoom: 15.5,
      cooperativeGestures: true, attributionControl: { compact: true },
    });
    map.on('error', (e) => console.warn('hero3d:', e.error?.message ?? e));
    return map;
  }

  const HERO3D = {
    ready: false,

    init() {
      if (!supported()) { setDom('lite'); return; }
      let saved = 'lite';
      try { saved = localStorage.getItem('hero-view') ?? 'lite'; } catch { /* egal */ }
      if (saved !== 'lite') this.setMode(saved);
      HERO3D.ready = true;
      document.querySelector('#hero3d-controls')?.removeAttribute('hidden');
    },

    getMode() { return mode; },

    setMode(newMode) {
      if (!['lite', '3d', 'chart'].includes(newMode) || newMode === mode) return;
      if (newMode === 'lite') { setDom('lite'); return; }
      const m = ensureMap();
      const want3d = newMode === '3d';
      const styleName = m.getStyle()?.layers?.some((l) => l.id === 'seekarte') ? 'chart' : '3d';
      if ((want3d ? '3d' : 'chart') !== styleName || mode === 'lite') {
        m.setStyle(want3d ? STYLE_3D : STYLE_CHART);
      }
      m.easeTo(want3d
        ? { pitch: PRESETS.schlei.pitch, bearing: PRESETS.schlei.bearing, duration: 600 }
        : { pitch: 0, bearing: 0, duration: 600 });
      m.once('idle', () => m.resize());
      setDom(newMode);
    },

    setPreset(name) {
      const p = PRESETS[name];
      if (!p || !map || mode === 'lite') return;
      map.flyTo({ ...p, ...(mode === 'chart' ? { pitch: 0, bearing: 0 } : {}), duration: 1800 });
    },
  };

  window.HERO3D = HERO3D;
  document.addEventListener('DOMContentLoaded', () => HERO3D.init());
})();
```

- [ ] **Step 2: index.html — Container und Scripts**

In `.fjord-svg-wrap` direkt **vor** `<svg id="fjord-svg"…>` einfügen:

```html
<div id="hero3d" aria-label="3D-Ansicht der Schlei"></div>
```

Nach `<p class="wind-wx" …>` einfügen:

```html
<p class="hero3d-caption" id="hero3d-caption" hidden>
  Seekarten-Darstellung aus BSH-Open-Data (FreeNauticalChart) —
  <strong>nicht zur Navigation</strong>.
</p>
```

Im `<head>` (bei den vorhandenen Referenzen):

```html
<link rel="stylesheet" href="vendor/maplibre-gl.css?v=__BUILD__">
<script src="vendor/maplibre-gl.js?v=__BUILD__" defer></script>
<script src="hero3d.js?v=__BUILD__" defer></script>
```

- [ ] **Step 3: styles.css — Sichtbarkeits-Umschaltung per data-Attribut**

An das Ende des Fjord-Blocks (nach den `.hero-zoom`-Regeln):

```css
/* 3D-Hero: Modus steuert, ob Silhouette oder MapLibre sichtbar ist */
#hero3d { position: absolute; inset: 0; opacity: 0; pointer-events: none; transition: opacity .4s; }
:root[data-hero-mode="3d"] #hero3d,
:root[data-hero-mode="chart"] #hero3d { opacity: 1; pointer-events: auto; }
:root[data-hero-mode="3d"] #fjord-svg,
:root[data-hero-mode="chart"] #fjord-svg,
:root[data-hero-mode="3d"] #wind-canvas,
:root[data-hero-mode="chart"] #wind-canvas,
:root[data-hero-mode="3d"] .hero-zoom,
:root[data-hero-mode="chart"] .hero-zoom { opacity: 0; pointer-events: none; transition: opacity .4s; }
.hero3d-caption { margin: 0; padding: 6px 2px 0; font-size: 12px; color: #8fa8b4; }
```

- [ ] **Step 4: Syntax- und Rauchtest**

```bash
node --check hero3d.js && python3 -m http.server 8734 &
```

Im headless Chrome (Verify-Rezept) laden und in der Konsole prüfen:
`HERO3D.ready === true`, `HERO3D.setMode('3d')` rendert (Canvas in `#hero3d` vorhanden).

- [ ] **Step 5: Commit**

```bash
git add hero3d.js index.html styles.css && git commit -m "hero3d: MapLibre-Grundszene mit Unterwasser-Relief und Seekarten-Stil"
```

---

### Task 6: Bedienelemente — Umschalter + Kamera-Presets

**Files:**
- Modify: `index.html` (Buttons)
- Modify: `styles.css` (Button-Reihe)
- Modify: `hero3d.js` (Button-Verdrahtung in `init()`)

**Interfaces:**
- Consumes: `HERO3D.setMode/setPreset/getMode` (Task 5).
- Produces: sichtbare UI; keine neue API.

- [ ] **Step 1: index.html — Bedienreihe in `.fjord-svg-wrap` (nach `.hero-zoom`)**

```html
<div class="hero3d-controls" id="hero3d-controls" hidden>
  <button type="button" id="hero3d-mode" aria-label="Ansicht wechseln">Ansicht: Klassisch</button>
  <span class="hero3d-presets" role="group" aria-label="Blickpunkte">
    <button type="button" data-preset="schlei">Ganze Schlei</button>
    <button type="button" data-preset="innere">Innere</button>
    <button type="button" data-preset="enge">Enge</button>
    <button type="button" data-preset="kappeln">Kappeln</button>
    <button type="button" data-preset="muendung">Mündung</button>
  </span>
</div>
```

- [ ] **Step 2: styles.css**

```css
.hero3d-controls {
  position: absolute; top: 10px; left: 10px;
  display: flex; flex-wrap: wrap; gap: 6px; max-width: 70%;
}
.hero3d-controls button {
  padding: 6px 10px; border-radius: 9px;
  border: 1px solid rgba(255, 255, 255, .18);
  background: rgba(13, 27, 34, .72); color: #d9e6ec;
  font-size: 12.5px; cursor: pointer;
}
.hero3d-controls button:hover { background: rgba(34, 56, 68, .85); }
.hero3d-presets { display: none; gap: 6px; }
:root[data-hero-mode="3d"] .hero3d-presets,
:root[data-hero-mode="chart"] .hero3d-presets { display: inline-flex; }
@media (max-width: 700px) { .hero3d-controls { max-width: 100%; } }
```

- [ ] **Step 3: hero3d.js — am Ende von `init()` verdrahten**

```javascript
      const MODES = ['lite', '3d', 'chart'];
      const NAMES = { lite: 'Klassisch', '3d': 'Revier 3D', chart: 'Seekarte' };
      const modeBtn = document.querySelector('#hero3d-mode');
      const syncBtn = () => {
        const next = MODES[(MODES.indexOf(mode) + 1) % MODES.length];
        modeBtn.textContent = `Ansicht: ${NAMES[next]}`;
      };
      modeBtn?.addEventListener('click', () => {
        this.setMode(MODES[(MODES.indexOf(mode) + 1) % MODES.length]);
        syncBtn();
      });
      syncBtn();
      document.querySelectorAll('.hero3d-presets [data-preset]').forEach((b) =>
        b.addEventListener('click', () => this.setPreset(b.dataset.preset)));
```

(Der Knopf zeigt jeweils die **nächste** Ansicht im Zyklus Klassisch → Revier 3D → Seekarte.)

- [ ] **Step 4: Rauchtest im Browser (Verify-Rezept), dann Commit**

Klickfolge: Umschalter 2× (3D, dann Seekarte), ein Preset, Umschalter 1× (zurück zu Klassisch). Erwartet: Überblendungen, keine Konsolenfehler.

```bash
git add index.html styles.css hero3d.js && git commit -m "hero3d: Ansicht-Umschalter und Kamera-Presets"
```

---

### Task 7: Verifikation Phase 1 + Verify-Skill/CLAUDE.md nachführen

**Files:**
- Create: Scratchpad `verify-hero3d.js` (nicht committen)
- Modify: `.claude/skills/verify/SKILL.md` (WebGL-Flags)
- Modify: `CLAUDE.md` (hero3d-Architekturabsatz)

**Interfaces:**
- Consumes: `HERO3D`-API, laufender `http.server`.

- [ ] **Step 1: Verify-Skript schreiben (Scratchpad)**

```javascript
const puppeteer = require('puppeteer-core');
const OUT = __dirname;

async function run(name, args, check) {
  const browser = await puppeteer.launch({
    executablePath: '/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',
    headless: 'new', args,
  });
  const page = await browser.newPage();
  const errs = [];
  page.on('pageerror', (e) => errs.push(e.message));
  await page.setViewport({ width: 1280, height: 900 });
  await page.goto('http://localhost:8734/index.html', { waitUntil: 'networkidle2', timeout: 45000 });
  await check(page, errs, name);
  console.log(name, 'PAGEERRORS:', errs.length ? errs.join(' | ') : '(keine)');
  await browser.close();
}

(async () => {
  // 1) WebGL-Lauf: 3D + Seekarte + Preset
  await run('webgl', ['--enable-unsafe-swiftshader', '--use-angle=swiftshader'], async (page) => {
    await page.waitForFunction(() => window.HERO3D?.ready === true, { timeout: 20000 });
    await page.evaluate(() => HERO3D.setMode('3d'));
    await page.waitForFunction(() => document.querySelector('#hero3d canvas'), { timeout: 20000 });
    await new Promise((r) => setTimeout(r, 6000));   // Kacheln + Terrain
    await page.screenshot({ path: `${OUT}/h1-3d.png` });
    await page.evaluate(() => HERO3D.setPreset('enge'));
    await new Promise((r) => setTimeout(r, 3000));
    await page.screenshot({ path: `${OUT}/h2-preset-enge.png` });
    await page.evaluate(() => HERO3D.setMode('chart'));
    await new Promise((r) => setTimeout(r, 5000));
    await page.screenshot({ path: `${OUT}/h3-seekarte.png` });
    console.log('caption sichtbar:', await page.evaluate(() => !document.querySelector('#hero3d-caption').hidden));
  });

  // 2) Fallback-Lauf: WebGL aus → Silhouette bleibt, Controls versteckt
  await run('fallback', ['--disable-webgl', '--disable-webgl2'], async (page) => {
    await new Promise((r) => setTimeout(r, 3000));
    console.log('fallback:', await page.evaluate(() => ({
      controlsHidden: document.querySelector('#hero3d-controls')?.hidden ?? true,
      svgSichtbar: getComputedStyle(document.querySelector('#fjord-svg')).opacity === '1',
    })));
    await page.screenshot({ path: `${OUT}/h4-fallback.png` });
  });
})().catch((e) => { console.error('FATAL', e); process.exit(1); });
```

- [ ] **Step 2: Laufen lassen und Screenshots begutachten**

```bash
python3 -m http.server 8734 &   # im Repo-Root
node verify-hero3d.js
```

Erwartet: `h1-3d.png` zeigt plastisches Relief mit Fahrrinne, `h3-seekarte.png` FNC-Karte + Caption `true`, Fallback-Lauf `controlsHidden: true, svgSichtbar: true`. Zusätzlich denselben WebGL-Lauf mit iPhone-Viewport (390×844) wiederholen. Falls `--use-angle=swiftshader` kein WebGL liefert: `--use-gl=swiftshader` bzw. `--use-angle=swiftshader-webgl` probieren und die funktionierende Variante in Step 3 dokumentieren.

- [ ] **Step 3: Verify-Skill und CLAUDE.md nachführen, Commit**

`.claude/skills/verify/SKILL.md`, Abschnitt „Fahren": die funktionierenden WebGL-Flags ergänzen. `CLAUDE.md`, Abschnitt „Architektur": neuer Punkt —

```markdown
- **hero3d.js**: MapLibre-Szene über der Silhouette (Modi lite/3d/chart via
  `data-hero-mode`, API `window.HERO3D`). Gelände aus gebackenen Kacheln
  (`terrain/`, `scripts/bake_terrain.py`), Wasser aus `water.geojson`.
  MapLibre liegt gepinnt in `vendor/`.
```

```bash
git add .claude/skills/verify/SKILL.md CLAUDE.md && git commit -m "Verify: WebGL-Rezept; CLAUDE.md: hero3d-Architektur"
```

---

## Self-Review (erledigt)

- **Spec-Abdeckung Phase 1:** Spike ✓ (T1), Kombi-Kacheln ✓ (T3), MapLibre ✓ (T4), dunkler 3D-Stil + halbtransparentes Wasser + Relief ✓ (T5), Presets + freie Kamera ✓ (T5/T6), Umschalter + localStorage + „nicht zur Navigation" ✓ (T5/T6), Fallback + Überblendung ✓ (T5), Verifikation ✓ (T7). Abweichung dokumentiert: Standard bleibt in Phase 1 `lite` (Global Constraints).
- **Platzhalter:** `TODO-aus-Spike` in Task 3 und `FNC`-Spanne in Task 5 sind bewusste Übergabepunkte aus Task 1 — im jeweiligen Step als solche benannt.
- **Typkonsistenz:** `HERO3D.setMode/setPreset/getMode/ready`, Modi `lite|3d|chart`, Preset-Namen und `data-hero-mode`-Werte in T5, T6 und T7 identisch.
