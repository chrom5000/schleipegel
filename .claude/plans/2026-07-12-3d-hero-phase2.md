# 3D-Hero Phase 2 (Datenlayer) — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Das Segler-Cockpit in der 3D-Szene: Pegel, Revierwind, Ostsee, Warnungen und Badestellen als Marker über beiden Ansichten, Zeitregler-Kopplung, Licht nach Sonnenstand — und 3D wird Standardansicht.

**Architecture:** Alle Datenlayer sind `maplibregl.Marker` (DOM-basiert): sie überleben `setStyle`-Wechsel, sind immer kameragerecht („Billboards") und nutzen das bestehende CSS-Idiom. `hero3d.js` bekommt einen idempotenten `renderData()`-Renderer, der direkt aus dem globalen `state` von app.js liest (klassische Scripts teilen den globalen Lexikalbereich; app.js lädt vor hero3d.js). app.js ruft `window.HERO3D?.renderData?.()` an zwei Hook-Punkten.

**Tech Stack:** MapLibre GL JS 5.24 (Marker, Popup, hillshade-illumination), bestehende app.js-Helfer (`classify`, `trendInfo`, `bftClass`, `compassPoint`, Formatter).

**Spec:** `.claude/specs/2026-07-12-3d-hero-design.md` · **Phase 1:** abgeschlossen (Commits f918be1…31640b0)

## Global Constraints

- Wie Phase 1 (kein Build, kein CDN, `?v=__BUILD__`, Deutsch, pushen nur auf Anweisung).
- **Rollout-Flip:** Standardansicht wird `3d` (wenn WebGL da und kein gespeicherter Nutzerwunsch); Silhouette bleibt First Paint bis `map.load` und dauerhafter Fallback.
- hero3d liest app.js-Globals: `state`, `STATIONS`, `SCHLEIMUENDE`, `classify`, `trendInfo`, `beaufort`, `bftClass`, `BFT_NAMES`, `compassPoint`, `fmtCm`, `fmtWind`, `fmtTemp`, `fmtTime`. Vor Nutzung per `typeof`-Guard absichern (hero3d darf ohne app.js nicht werfen).
- Marker-Chips: dunkler Hintergrund `rgba(13,27,34,.82)`, damit sie auf 3D (dunkel) und Seekarte (hell) gleichermaßen lesbar sind.

---

### Task 1: 3D als Standard + weiche Bereitschafts-Übergabe + Presets mobil einzeilig

**Files:**
- Modify: `hero3d.js` (init/setMode)
- Modify: `styles.css` (Presets scrollbar)

**Interfaces:**
- Produces: `setMode` blendet erst nach `map`-`load` um (kein schwarzes Loch beim Autostart); Standard `3d`.

- [ ] **Step 1: hero3d.js — Standard und Lade-Übergabe**

In `init()` die saved-Logik ersetzen:

```javascript
      let saved = null;
      try { saved = localStorage.getItem('hero-view'); } catch { /* egal */ }
      this.setMode(saved ?? '3d');                 // Standard: Revier 3D
```

In `setMode()` das sofortige `setDom` durch Warten auf Kartenbereitschaft ersetzen:

```javascript
      m.once('idle', () => m.resize());
      if (m.loaded()) setDom(newMode);
      else m.once('load', () => setDom(newMode)); // Silhouette steht, bis die Szene da ist
      if (typeof this.renderData === 'function') {
        if (m.loaded()) this.renderData();
        else m.once('load', () => this.renderData());
      }
```

(`setDom(newMode)`-Aufruf an alter Stelle entfernen. `mode` wird weiterhin erst in `setDom` gesetzt — bis dahin bleibt `lite` aktiv; damit `setMode` nicht doppelt startet, am Methodenanfang zusätzlich `if (this._pending === newMode) return; this._pending = newMode;` und in `setDom` `HERO3D._pending = null;`.)

- [ ] **Step 2: styles.css — Presets einzeilig scrollbar (mobil)**

```css
@media (max-width: 700px) {
  .hero3d-controls { max-width: 100%; flex-wrap: nowrap; overflow-x: auto; padding-bottom: 2px; }
  .hero3d-controls button { white-space: nowrap; }
}
```

(ersetzt die bestehende `@media`-Regel für `.hero3d-controls`)

- [ ] **Step 3: Prüfen + Commit**

`node --check hero3d.js`; im headless Chrome (WebGL-Flags): frischer Kontext ohne localStorage → nach Laden wechselt die Ansicht automatisch auf 3D, vorher Silhouette sichtbar.

```bash
git add hero3d.js styles.css && git commit -m "hero3d: Revier 3D als Standardansicht, Übergabe erst bei Kartenbereitschaft"
```

---

### Task 2: Marker-Infrastruktur + Pegel-Marker

**Files:**
- Modify: `hero3d.js` (Marker-Verwaltung, `renderData`, Pegel)
- Modify: `styles.css` (Marker-Chips, Pegellatte)
- Modify: `app.js` (2 Hook-Aufrufe)

**Interfaces:**
- Produces: `HERO3D.renderData()` — idempotent, no-op wenn Karte nicht bereit oder Modus `lite`. Interne Helfer `upsertMarker(key, lngLat, el, opts)` / `dropMarker(key)` für Task 3+4.
- Consumes: `state.current`, `STATIONS` (je `charVals`, `lat`, `lon`), `classify`, `trendInfo`, `fmtCm`.

- [ ] **Step 1: hero3d.js — Marker-Verwaltung und Pegel-Renderer (vor `const HERO3D`)**

```javascript
  /* ── DATENLAYER (Phase 2): DOM-Marker, überleben setStyle ── */

  const markers = new Map();   // key → maplibregl.Marker

  function upsertMarker(key, lngLat, el, opts = {}) {
    let mk = markers.get(key);
    if (!mk) {
      mk = new maplibregl.Marker({ element: el, ...opts }).setLngLat(lngLat).addTo(map);
      markers.set(key, mk);
    } else {
      mk.setLngLat(lngLat);
      if (opts.rotation != null) mk.setRotation(opts.rotation);
    }
    return mk;
  }

  function dropMarker(key) {
    markers.get(key)?.remove();
    markers.delete(key);
  }

  function renderPegel() {
    if (typeof STATIONS === 'undefined') return;
    for (const st of STATIONS) {
      const cur = state.current?.[st.id];
      const key = `pegel:${st.id}`;
      if (!cur) { dropMarker(key); continue; }
      const cls = classify(cur.value, st.charVals);
      const tr = trendInfo(cur.trend ?? 0);
      const span = st.charVals.MHW - st.charVals.MNW;
      const p = Math.max(0, Math.min(1, (cur.value - st.charVals.MNW) / span));
      let el = markers.get(key)?.getElement();
      if (!el) {
        el = document.createElement('div');
        el.className = 'h3-marker h3-pegel';
        el.innerHTML = '<span class="h3-latte"><i></i></span><span class="h3-body"><b></b><small></small></span>';
      }
      el.querySelector('b').textContent = `${fmtCm.format(cur.value)} cm ${tr.arrow}`;
      el.querySelector('small').textContent = `Pegel ${st.name} · ${cls.label}`;
      const bar = el.querySelector('.h3-latte i');
      bar.style.height = `${Math.round(p * 100)}%`;
      bar.style.background = cls.color;
      el.title = `Pegel ${st.name}: ${fmtCm.format(cur.value)} cm (${cls.label}, ${tr.txt})`;
      upsertMarker(key, [st.lon, st.lat], el, { anchor: 'bottom' });
    }
  }
```

Und die (in Task 1 bereits referenzierte) Methode am `HERO3D`-Objekt:

```javascript
    renderData() {
      if (!map || !map.loaded() || mode === 'lite' || typeof state === 'undefined') return;
      renderPegel();
    },
```

- [ ] **Step 2: styles.css — Chips und Pegellatte**

```css
/* hero3d-Datenmarker: dunkle Chips, lesbar auf 3D und Seekarte */
.h3-marker {
  display: flex; align-items: flex-end; gap: 6px;
  padding: 5px 8px; border-radius: 9px;
  background: rgba(13, 27, 34, .82);
  border: 1px solid rgba(255, 255, 255, .16);
  color: #f2f7f8; font-family: var(--font-viz); line-height: 1.25;
  pointer-events: auto; cursor: default;
}
.h3-marker b { display: block; font-size: 14px; }
.h3-marker small { display: block; font-size: 10.5px; color: #9fb6c1; }
.h3-latte {
  width: 7px; height: 34px; border-radius: 3px;
  background: rgba(255, 255, 255, .14); position: relative; overflow: hidden;
}
.h3-latte i { position: absolute; bottom: 0; left: 0; right: 0; display: block; }
@media (max-width: 700px) {
  .h3-marker b { font-size: 15px; }
}
```

- [ ] **Step 3: app.js — Hooks (2 Zeilen)**

Am Ende von `renderTiles()`: `window.HERO3D?.renderData?.();`
Am Ende von `renderRevierWind()`: `window.HERO3D?.renderData?.();`

- [ ] **Step 4: Prüfen + Commit**

Headless: 3D-Modus, warten auf Marker (`document.querySelectorAll('.h3-pegel').length === 2`), Screenshot; Seekarten-Modus → Marker weiterhin da.

```bash
git add hero3d.js styles.css app.js && git commit -m "hero3d: Pegel-Marker mit Latte, Einstufung und Tendenz"
```

---

### Task 3: Revierwind-Pfeile mit Zeitregler-Kopplung

**Files:**
- Modify: `hero3d.js`

**Interfaces:**
- Consumes: `state.revierWind` (`points[].hours[]`, `times`), `state.revierIdx`, `bftClass`, `beaufort`, `BFT_NAMES`, `compassPoint`, `fmtWind`, `fmtTemp`. Konvention: Pfeilgrundform zeigt nach Süden, Rotation = `dir` (CLAUDE.md).
- Produces: Marker `wind:<name>` mit `rotationAlignment:'map'` (dreht mit der Karte), Chip mit m/s-Wert.

- [ ] **Step 1: hero3d.js — Wind-Renderer**

```javascript
  const VANE_COLORS = { w1: '#8fc3e0', w2: '#d8ebf4', w3: '#ffd97a', w4: '#ff8f5e' };

  function renderWind() {
    const rw = state.revierWind;
    if (!rw) { for (const k of [...markers.keys()]) if (k.startsWith('wind:')) dropMarker(k); return; }
    const idx = Math.min(state.revierIdx ?? 0, rw.times.length - 1);
    for (const p of rw.points) {
      const h = p.hours[idx];
      const key = `wind:${p.name}`;
      if (!h || h.ms == null || h.dir == null) { dropMarker(key); continue; }
      const color = VANE_COLORS[bftClass(h.ms)];
      let el = markers.get(key)?.getElement();
      if (!el) {
        el = document.createElement('div');
        el.className = 'h3-wind';
        el.innerHTML = `<svg viewBox="-10 -22 20 44" width="26" height="56" aria-hidden="true">
            <line class="h3-vane-shaft" x1="0" y1="-18" x2="0" y2="12"/>
            <path class="h3-vane-head" d="M-6,8 L0,20 L6,8 Z"/>
          </svg><b></b>`;
      }
      el.querySelector('b').textContent = fmtWind.format(h.ms);
      el.querySelector('.h3-vane-shaft').style.stroke = color;
      el.querySelector('.h3-vane-head').style.fill = color;
      el.querySelector('b').style.color = color;
      const bft = beaufort(h.ms);
      el.title = `${p.name}: ${fmtWind.format(h.ms)} m/s (${bft} Bft, ${BFT_NAMES[bft]}) aus ${compassPoint(h.dir)}, Böen ${fmtWind.format(h.gust ?? 0)} m/s${h.temp != null ? `, ${fmtTemp.format(h.temp)} °C` : ''} — ICON-D2`;
      upsertMarker(key, [p.lon, p.lat], el, { rotationAlignment: 'map', rotation: h.dir });
    }
  }
```

Der `b`-Wert (Zahl) würde mitrotieren — deshalb rotiert nur der Marker (Pfeil-SVG), die Zahl wird gegenrotiert:

```javascript
      el.querySelector('b').style.transform = `rotate(${-h.dir}deg)`;
```

(direkt nach dem Setzen des Textes; zusätzlich gleicht `rotationAlignment:'map'` das Karten-Bearing automatisch aus — Prüfpunkt in Step 3: Zahl bleibt bei gedrehter Karte aufrecht? Falls nicht, Bearing in die Gegenrotation einbeziehen: `-h.dir - map.getBearing()`, aktualisiert im `rotate`-Event.)

In `renderData()` ergänzen: `renderWind();`

- [ ] **Step 2: styles.css**

```css
.h3-wind { display: grid; justify-items: center; pointer-events: auto; }
.h3-wind svg { filter: drop-shadow(0 0 3px rgba(0, 0, 0, .8)); }
.h3-vane-shaft { stroke-width: 3.5; stroke-linecap: round; }
.h3-wind b {
  font-family: var(--font-viz); font-size: 13px;
  text-shadow: 0 0 4px rgba(0, 0, 0, .9), 0 0 8px rgba(0, 0, 0, .7);
}
```

- [ ] **Step 3: Prüfen + Commit**

Headless: 3D-Modus, 7 `.h3-wind`-Marker; Zeitregler auf 24 setzen (`input`-Event) → Werte in den Chips ändern sich; Karte per `map.rotateTo(90)` drehen → Pfeile zeigen weiter geographisch korrekt (Screenshot-Vergleich), Zahlen aufrecht.

```bash
git add hero3d.js styles.css && git commit -m "hero3d: Revierwind-Pfeile, an Zeitregler und Kartendrehung gekoppelt"
```

---

### Task 4: Ostsee-Badge, Warn-Marker, Badestellen

**Files:**
- Modify: `hero3d.js`

**Interfaces:**
- Consumes: `state.marine` (`sst`, `wave`), `state.alerts` (Array mit `.event`), `state.badewasser.spots` (`name`, `lat`, `lon`, `ecoli`, `entero`, `datum`, `wasserTemp`), `SCHLEIMUENDE`, `fmtCm`.
- Produces: Marker `marine:ostsee`, `alert:0`, `bw:<name>`; Badestellen mit MapLibre-Popup.

- [ ] **Step 1: hero3d.js — Renderer**

```javascript
  function renderExtra() {
    // Ostsee vor Schleimünde
    const mr = state.marine;
    if (mr) {
      const key = 'marine:ostsee';
      let el = markers.get(key)?.getElement();
      if (!el) {
        el = document.createElement('div');
        el.className = 'h3-marker h3-marine';
        el.innerHTML = '<span class="h3-body"><b></b><small>Ostsee · Welle · Wasser</small></span>';
      }
      el.querySelector('b').textContent = `${fmtCm.format(mr.wave ?? 0)} m · ${fmtCm.format(mr.sst)} °C`;
      el.title = `Ostsee vor Schleimünde: Wellenhöhe ${fmtCm.format(mr.wave ?? 0)} m, Wassertemperatur ${fmtCm.format(mr.sst)} °C`;
      upsertMarker(key, [10.075, 54.685], el, { anchor: 'left' });
    } else dropMarker('marine:ostsee');

    // Amtliche Warnung als Szene-Marker (Banner oben existiert weiter)
    if (state.alerts?.length) {
      const key = 'alert:0';
      let el = markers.get(key)?.getElement();
      if (!el) {
        el = document.createElement('div');
        el.className = 'h3-marker h3-alert';
        el.innerHTML = '<span class="h3-body"><b>⚠︎ Warnung</b><small></small></span>';
      }
      el.querySelector('small').textContent = state.alerts[0].event ?? 'amtliche Warnung';
      upsertMarker(key, [9.79, 54.60], el, { anchor: 'bottom' });
    } else dropMarker('alert:0');

    // Badestellen als klickbare Ampel-Punkte mit Popup
    for (const s of state.badewasser?.spots ?? []) {
      const key = `bw:${s.name}`;
      if (markers.has(key)) continue;                      // statisch je Seitenladen
      const ok = (s.ecoli == null || s.ecoli <= 500) && (s.entero == null || s.entero <= 200);
      const el = document.createElement('button');
      el.type = 'button';
      el.className = 'h3-bw';
      el.style.background = ok ? '#0ca30c' : '#fab219';
      el.setAttribute('aria-label', `Badestelle ${s.name}`);
      const detail = s.datum
        ? `Probe ${new Date(s.datum).toLocaleDateString('de-DE')}: E. coli ${s.ecoli ?? '–'}, Enterokokken ${s.entero ?? '–'} · ${ok ? 'unauffällig' : 'erhöht'}`
        : 'keine aktuelle Probe';
      upsertMarker(key, [s.lon, s.lat], el)
        .setPopup(new maplibregl.Popup({ offset: 10, closeButton: false })
          .setText(`${s.name} — ${detail}`));
    }
  }
```

In `renderData()` ergänzen: `renderExtra();`

- [ ] **Step 2: styles.css**

```css
.h3-bw {
  width: 13px; height: 13px; border-radius: 50%;
  border: 2px solid rgba(13, 27, 34, .9); cursor: pointer; padding: 0;
}
.h3-marker.h3-alert { border-color: var(--st-warn); }
.h3-marker.h3-alert b { color: var(--st-warn); }
#hero3d .maplibregl-popup-content {
  background: rgba(13, 27, 34, .95); color: #f2f7f8;
  font-family: var(--font-viz); font-size: 12.5px; border-radius: 9px;
}
#hero3d .maplibregl-popup-tip { border-top-color: rgba(13, 27, 34, .95); }
```

- [ ] **Step 3: Prüfen + Commit**

Headless: Marine-Badge vorhanden (`.h3-marine`), Badestellen-Punkte > 10, Klick auf einen → Popup mit Probenwerten. Warn-Marker nur prüfbar, wenn Warnung aktiv — per Konsole `state.alerts = [{event:'Test-Sturmflut'}]; HERO3D.renderData()` simulieren, Marker erscheint, danach zurücksetzen.

```bash
git add hero3d.js styles.css && git commit -m "hero3d: Ostsee-Badge, Warn-Marker und Badestellen mit Popups"
```

---

### Task 5: Licht nach Sonnenstand

**Files:**
- Modify: `hero3d.js`
- Modify: `app.js` (1 Hook-Zeile)

**Interfaces:**
- Consumes: app.js-Sonnenrechnung — Funktionsnamen vor Implementierung per `grep -n 'function sun' app.js` verifizieren (erwartet: eine Funktion, die `{az, el}` zur Zeit liefert; in renderSunLayer als `cur.az/cur.el` genutzt).
- Produces: `HERO3D.renderLight()` — setzt `hillshade-illumination-direction` auf den Sonnen-Azimut und dunkelt die Szene nachts ab.

- [ ] **Step 1: hero3d.js**

```javascript
    renderLight() {
      if (!map || !map.loaded() || !map.getLayer('relief')) return;
      if (typeof sunPosition !== 'function') return;       // Namen ggf. anpassen (siehe Interfaces)
      const sun = sunPosition(new Date());
      const up = sun.el > -3;
      map.setPaintProperty('relief', 'hillshade-illumination-direction',
        up ? ((sun.az % 360) + 360) % 360 : 315);
      map.setPaintProperty('relief', 'hillshade-highlight-color', up ? '#4b7d9b' : '#22404f');
      map.setPaintProperty('bg', 'background-color', up ? '#0d1b22' : '#070f14');
    },
```

Aufruf zusätzlich am Ende von `renderData()`.

- [ ] **Step 2: app.js — Hook**

Am Ende von `renderSunLayer()` (läuft minütlich): `window.HERO3D?.renderLight?.();`

- [ ] **Step 3: Prüfen + Commit**

Headless: `map.getPaintProperty('relief','hillshade-illumination-direction')` entspricht dem aktuellen Sonnen-Azimut (nachmittags West ≈ 230–280°).

```bash
git add hero3d.js app.js && git commit -m "hero3d: Lichtstimmung folgt dem Sonnenstand"
```

---

### Task 6: Phase-2-Verifikation + Doku

**Files:**
- Modify: Scratchpad `verify-hero3d.js`
- Modify: `CLAUDE.md`, `.claude/plans/2026-07-12-3d-hero-phase2.md` (Checkboxen)

- [ ] **Step 1: verify-hero3d.js erweitern**

Desktop-WebGL-Lauf ergänzt: frischer Kontext → Standard wird 3D (ohne Klick); `.h3-pegel` = 2, `.h3-wind` = 7, `.h3-bw` > 10, `.h3-marine` = 1; Zeitregler 0→24 ändert Wind-Chip-Werte; Wechsel zu Seekarte → Marker bleiben; `rotateTo(90)` → Screenshot (Pfeile geographisch korrekt); Licht-Property gesetzt. iPhone- und Fallback-Läufe unverändert.

- [ ] **Step 2: Screenshots begutachten** (3D-Cockpit Desktop + iPhone, Seekarte mit Markern, gedrehte Karte)

- [ ] **Step 3: CLAUDE.md ergänzen (hero3d-Datenlayer, Hooks) + Commit**

```bash
git add CLAUDE.md .claude/plans/2026-07-12-3d-hero-phase2.md && git commit -m "Doku: hero3d-Datenlayer und app.js-Hooks"
```

---

## Self-Review (erledigt)

- **Spec-Abdeckung Phase 2:** Pegel ✓ (T2), Revierwind + Zeitregler ✓ (T3), Ostsee ✓ (T4), Warnungen ✓ (T4), Badestellen ✓ (T4), Licht ✓ (T5), Standard-Flip ✓ (T1). Wetterzeile: bleibt HTML (kein Task nötig, Spec-konform).
- **Platzhalter:** Sonnenfunktions-Name ist als expliziter Verifikationspunkt markiert (Interfaces T5) — kein blinder Verweis.
- **Typkonsistenz:** `upsertMarker/dropMarker/markers` (T2) werden in T3/T4 identisch verwendet; `renderData` ruft `renderPegel/renderWind/renderExtra/renderLight` in dieser Reihenfolge.
